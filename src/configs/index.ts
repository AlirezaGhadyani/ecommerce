const appConfig: AppConfig = {
  locales: ["en", "fa"],
  defaultLocale: "en",
  ui: {
    admin: {
      sidebarOpenWidth: 240,
      sidebarCloseWidth: 80,
    },
  },
};

export default appConfig;
